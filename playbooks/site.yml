---
# Playbook principal — composants déjà fusionnés dans main
# Nginx (José) et sécurité (Chris) seront ajoutés après leurs PR

- name: Vérifier la connectivité de toutes les machines
  hosts: all
  gather_facts: false
  tasks:
    - name: Tester la connexion Ansible
      ansible.builtin.ping:

- import_playbook: users.yml
- import_playbook: install_apache.yml
- import_playbook: monitoring.yml
